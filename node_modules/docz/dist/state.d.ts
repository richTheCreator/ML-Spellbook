import { ComponentType as CT } from 'react';
import { ComponentsMap } from './components/DocPreview';
export declare type MSXComponent = CT<{
    components: ComponentsMap;
}>;
export interface MSXImport {
    default: MSXComponent;
    getInitialData?: (props: any) => any;
}
export interface Heading {
    depth: number;
    slug: string;
    value: string;
}
export interface Entry {
    id: string;
    filepath: string;
    slug: string;
    route: string;
    name: string;
    order: number;
    menu: string | null;
    headings: Heading[];
    [key: string]: any;
}
export interface ThemeConfig {
    [key: string]: any;
}
export interface Config {
    title: string;
    description: string;
    ordering: string;
    themeConfig: ThemeConfig;
    version: string | null;
    repository: string | null;
    native: boolean;
}
declare type Import = () => Promise<MSXImport>;
export declare type EntryMap = Record<string, Entry>;
export declare type ImportMap = Record<string, Import>;
export declare type TransformFn = (config: Config) => Config;
export interface State {
    config: Config;
    entries: EntryMap;
}
export declare const state: any;
export {};
